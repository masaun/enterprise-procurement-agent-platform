// Intentionally empty. See package.json description / ../README.md.
// If this ever actually gets imported at runtime, that means something
// started using @lucid-agents/wallet's ThirdwebWalletConnector — replace
// the "thirdweb" override in package.json with the real package instead.
module.exports = {};
